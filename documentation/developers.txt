Info for developers


Notifications are made like this :
flash()->error('Task was unsuccessful!');
flash()->success( 'Task was successful!');
flash()->info('Everything is ok');

They are shown on every view.
Errors are also shown the same way using the standard error object.
